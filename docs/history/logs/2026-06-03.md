# 2026-06-03

## Tenerife Mountain Tree Visibility Fix

- Responded to user report that the newly added mountain trees were not visible.
- Diagnosed the renderer setup against Babylon's active mesh/render path: tree source meshes were left with `isVisible = false`, which prevents active mesh selection even when thin-instance matrices exist.
- Updated `TenerifeMountainTrees` so imported source meshes stay hidden only until thin-instance transforms are ready.
- After applying tree instance matrices, the renderer now refreshes thin-instance bounding info and re-enables mesh visibility for rendering, while keeping picking disabled and freezing bounds sync after the one-time refresh.
- Added focused regression coverage for source mesh hiding and instance mesh activation.
- Validation passed:
	- `bun run test -- src/scenes/environment/TenerifeMountainTrees.test.ts src/scenes/environment/tenerifeMountainTreeData.test.ts`
	- `bun run check src/scenes/environment/TenerifeMountainTrees.tsx src/scenes/environment/TenerifeMountainTrees.test.ts src/scenes/environment/tenerifeMountainTreeData.ts src/scenes/environment/tenerifeMountainTreeData.test.ts src/scenes/environment/Environment.tsx`
	- `bun run build`
	- `bun run test`

## Dense Tenerife Mountain Tree Coverage

- Responded to user request for many more trees across all mountain bands, above Puerto de la Cruz, and along the ridges from Puerto de la Cruz toward Santa Cruz while keeping Teide's desert zone sparse.
- Expanded the authored Tenerife mountain tree data from the first-pass sparse list to 258 deterministic placements.
- Added named dense belts for Teno, south ridge, central mountains, north mountains, Puerto ridge, Puerto-to-Santa-Cruz ridge, and the north-east crown.
- Preserved the 390m Teide dry-zone exclusion and fixed one near-boundary Teno placement found by the dry-zone regression test.
- Added regression coverage for dense mountain belt counts and Puerto/Santa-Cruz ridge extents.
- Validation passed:
	- `bun run test -- src/scenes/environment/tenerifeMountainTreeData.test.ts src/scenes/environment/TenerifeMountainTrees.test.ts`
	- `bun run check src/scenes/environment/tenerifeMountainTreeData.ts src/scenes/environment/tenerifeMountainTreeData.test.ts src/scenes/environment/TenerifeMountainTrees.tsx src/scenes/environment/TenerifeMountainTrees.test.ts src/scenes/environment/Environment.tsx`
	- `bun run test`
	- `bun run build`
- HTTP smoke passed for the Tenerife full-island dev URL and spruce GLB asset on `http://127.0.0.1:5175`.
- Browser visual QA was attempted but could not run because the Browser `iab` backend is unavailable in this session.

## Tenerife Mountain Tree Slope Correction

- Responded to user screenshot showing trees reading as a side/coastal band rather than on the visible mountain slopes.
- Parsed the full-island GLB terrain bounds and identified that the dense follow-up relied on guessed X/Z belts, including northern-edge placements around `z=-650..-700`.
- Recorded placement strategy analysis in `TASKS/tenerife-mountain-trees/slope-placement-analysis.md`.
- Chose an offline GLB-sampled static slope strategy over manual retuning or runtime random search.
- Replaced guessed belts with 192 deterministic slope-sampled placements:
	- `puerto-upper-slope`
	- `puerto-inner-slope`
	- `puerto-santa-cruz-slope`
	- `central-slope`
	- `teno-slope`
	- `south-slope`
- Added terrain sample metadata to every tree placement so tests assert height, slope, Teide dry-zone exclusion, northern-edge exclusion, and east-edge exclusion.
- Validation passed:
	- `bun run test -- src/scenes/environment/tenerifeMountainTreeData.test.ts src/scenes/environment/TenerifeMountainTrees.test.ts`
	- `bun run check src/scenes/environment/tenerifeMountainTreeData.ts src/scenes/environment/tenerifeMountainTreeData.test.ts src/scenes/environment/TenerifeMountainTrees.tsx src/scenes/environment/TenerifeMountainTrees.test.ts src/scenes/environment/Environment.tsx`
	- `bun run test`
	- `bun run build`
- HTTP smoke passed for the Tenerife full-island dev URL and spruce GLB asset on `http://127.0.0.1:5175`.
- Browser visual QA was attempted again but could not run because the Browser `iab` backend is unavailable in this session.

## Runtime Puerto Mountain Forest And Asset Grounding

- Responded to user report that some trees were floating and that the visible mountains above Puerto de la Cruz still lacked trees.
- Inspected `Tree.glb` and found it contains source-offset `mini_tree_*` roots, loose `dry branch` roots, and many separate branch roots rather than one clean runtime tree mesh.
- Updated `TenerifeMountainTrees` to hide all imported roots, render only main tree source roots, skip `mini_tree_*` and `dry branch` roots, and anchor the instanced tree source by the `tree trunk` root instead of the lowest stray branch vertex.
- Replaced static slope tuple placement with deterministic runtime heightfield scanning after the full-island terrain is ready.
- Added dense Puerto-prioritized forest regions around the eastern terrain edge and the Puerto-to-Santa-Cruz ridge chain, while retaining central, Teno, and south mountain regions outside the Teide dry zone.
- Added regression coverage for runtime forest generation, Puerto-edge density, Teide exclusion, slope/height acceptance, source-root filtering, and trunk-anchor detection.
- Validation passed:
	- `bun run test -- src/scenes/environment/tenerifeMountainTreeData.test.ts src/scenes/environment/TenerifeMountainTrees.test.ts`
	- `bun run check src/scenes/environment/tenerifeMountainTreeData.ts src/scenes/environment/tenerifeMountainTreeData.test.ts src/scenes/environment/TenerifeMountainTrees.tsx src/scenes/environment/TenerifeMountainTrees.test.ts src/scenes/environment/Environment.tsx`
	- `bun run test`
	- `bun run build`
- HTTP smoke passed for the Tenerife full-island dev URL and spruce GLB asset on `http://127.0.0.1:5175`.
- Browser visual QA was attempted again but could not run because the Browser `iab` backend is unavailable in this session.

## Tenerife Island-Full Startup Performance

- Responded to user report that `http://localhost:5173/js-game-tf-lang-run/?tenerife=1&terrain=island-full` eventually loads but can take several minutes.
- Created `TASKS/tenerife-island-full-startup-performance/` with product plan, roadmap, technical plan, and checklist.
- Identified the default `terrain=island-full` path as loading/generated Puerto road data by default:
	- `roads-runtime.json`: about 451 KB, 3427 line strings, 12902 road points.
	- `puerto-building-footprints-runtime.json`: about 377 KB, 1400 buildings.
- Changed full-island default road rendering from `mesh` to `none`, keeping road meshes explicit through `&roads=mesh` or `&roads=both`.
- Updated `Environment` so Tenerife runtime road data is not fetched/parsed/generated unless the active layer plan renders road meshes.
- Updated Puerto config regression tests for the new fast default and explicit road/building opt-ins.
- Validation passed:
	- `bun run test -- src/scenes/environment/puertoCityConfig.test.ts src/scenes/environment/tenerifeRoadLayers.test.ts src/scenes/environment/tenerifeFullIslandConfig.test.ts`
	- `bun run check`
	- `bun run test`
	- `bun run build`
- HTTP smoke passed for `http://localhost:5173/js-game-tf-lang-run/?tenerife=1&terrain=island-full`.
- Browser visual QA remained blocked by unavailable Browser `iab` and the DevTools MCP Chrome profile conflict.
	- `bun run check`
	- `bun run test`
	- `bun run build`
- HTTP smoke passed for `http://localhost:5173/js-game-tf-lang-run/?tenerife=1&terrain=island-full`.
- Browser visual QA was attempted but blocked: Browser `iab` was unavailable and DevTools MCP reported an already-running Chrome profile.

## Tenerife Island-Full Overlay Restoration

- Responded to user feedback that the first startup performance change removed roads and houses.
- Restored default `terrain=island-full` road rendering to `roads=mesh`, so Puerto roads and generated roadside houses remain visible without extra query params.
- Changed the performance strategy from disabling the overlay to delaying full-island road/house overlay loading until after the full-island terrain is ready.
- Added heightfield-first road grounding for the default full-island path, avoiding thousands of startup terrain raycasts while keeping `&roadGrounding=raycast` available for exact visual QA.
- Kept generated roadside houses visual-only by removing Havok bodies for that overlay path.
- Updated regression coverage for default visible roads/houses, heightfield grounding, explicit raycast grounding, and opt-out road mode.
- Validation passed:
	- `bun run test -- src/scenes/environment/puertoCityConfig.test.ts src/scenes/environment/tenerifeRoadLayers.test.ts src/scenes/environment/tenerifeFullIslandConfig.test.ts`
	- `bun run check`
	- `bun run test`
	- `bun run build`
- HTTP smoke passed for `http://localhost:5173/js-game-tf-lang-run/?tenerife=1&terrain=island-full`.
- Browser visual QA remained blocked by unavailable Browser `iab` and the DevTools MCP Chrome profile conflict.

## Tenerife Full-Island Road Grounding Correction

- Responded to user screenshot showing restored Puerto roads floating above the full-island terrain instead of reading as part of the ground.
- Identified the regression as the heightfield-first road grounding default: it is too coarse for visible sloped terrain and can place broad road ribbons above the mesh.
- Restored terrain raycast grounding as the default for `terrain=island-full` roads, while keeping the cheaper heightfield mode available through `&roadGrounding=heightfield` for performance comparison.
- Kept the previous performance protection that delays full-island road/house overlay loading until after island terrain readiness.
- Validation passed:
	- `bun run test -- src/scenes/environment/puertoCityConfig.test.ts src/scenes/environment/tenerifeRoadLayers.test.ts src/scenes/environment/tenerifeFullIslandConfig.test.ts`

## Tenerife Mountain Tree Visual Afforestation Reset

- Implemented the screenshot-driven follow-up for trees reading as an ocean/side band while Puerto-side mountains remained bare.
- Replaced the previous Puerto edge-biased forest region with Puerto-visible mountain face regions:
	- `puerto-visible-slopes`
	- `puerto-inner-ridges`
	- `puerto-santa-cruz-ridges`
- Added an explicit north-east ocean-edge rejection helper so generated trees cannot populate the band that reads as water or detached horizon from Puerto view.
- Added exported Puerto-visible zone helpers and regression coverage requiring dense Puerto-visible placement, zero ocean-edge placements, continued Teide dry-zone exclusion, and continued non-Puerto mountain fill.
- Validation passed:
	- `bun run test -- src/scenes/environment/tenerifeMountainTreeData.test.ts src/scenes/environment/TenerifeMountainTrees.test.ts`
	- `bun run check`
	- `bun run test`
	- `bun run build`
- HTTP smoke passed on `http://127.0.0.1:5175/js-game-tf-lang-run/?tenerife=1&terrain=island-full`.
- HTTP smoke passed for the spruce `Tree.glb` asset and the full-island terrain GLB with `200 OK`.
- Browser visual QA was attempted but blocked because the Browser `iab` backend is unavailable in this session; local Playwright is also not installed.

## Tenerife Mountain Tree Water Placement Hotfix

- Responded to user screenshot showing the phase 9 tree placement still rendering a dense forest in the ocean.
- Identified the runtime failure as `TenerifeMountainTrees` preferring full-island heightfield samples without requiring a terrain raycast confirmation or water-surface clearance.
- Updated tree grounding so each visible instance requires a full-island terrain raycast hit and rejects ground below `TENERIFE_FULL_ISLAND_WATER_SURFACE_Y` plus a safety margin.
- Kept heightfield data as an optional refinement only after the raycast proves the placement is on visible terrain above water.
- Added regression coverage for the water-height guard.
- Validation passed:
	- `bun run test -- src/scenes/environment/tenerifeMountainTreeData.test.ts src/scenes/environment/TenerifeMountainTrees.test.ts`
	- `bun run check`
	- `bun run test`
	- `bun run build`

## Tenerife Island Mountain Afforestation Retarget

- Responded to user clarification that the goal is to place trees across the island, especially the mountain areas, not to keep chasing the Puerto edge.
- Parsed `public/models/environment/tenerife-full-island-normalized.glb` terrain tile bounds and found the problematic eastern low/coastal tile:
	- `x=61..407`
	- `z=-741..-330`
	- maximum height about `20m`
- Removed that eastern water-facing sector from generation by capping safe tree placement at `x <= 60`.
- Replaced Puerto-edge tree regions with island mountain belts:
	- `north-visible-mountains`
	- `north-inner-ridges`
	- `north-east-inner-ridges`
	- `central-mountains`
	- `teno-mountains`
	- `south-mountains`
	- `west-mountain-slopes`
- Added regression coverage requiring dense north mountain coverage, no trees in the eastern water-facing sector, continued Teide dry-zone exclusion, and continued non-north mountain fill.
- Validation passed:
	- `bun run test -- src/scenes/environment/tenerifeMountainTreeData.test.ts src/scenes/environment/TenerifeMountainTrees.test.ts`
	- `bun run check`
	- `bun run test`
	- `bun run build`
