# 2026-06-02

## Full-Island Roadside House Grounding

- Fixed the remaining floating placeholder house issue reported from a full-island Tenerife screenshot.
- Added `worldBuildingGrounding.ts` to sample building support from the rotated footprint center, corners, and edge points instead of a single center point.
- Updated `WorldBuildings` so fallback boxes, thin-instance models, and primitive colliders use footprint-based support height when a height provider or terrain raycast is available.
- Raycast-dependent fallback boxes now stay hidden and non-pickable until they resolve a valid terrain hit, preventing visible floating boxes and invisible movement blockers.
- Added a small full-island generated-house ground sink in `Environment` so flat blockout foundations slightly embed into sloped terrain rather than leaving downhill gaps.
- Updated `TASKS/roadside-house-placement/` notes, roadmap, product plan, and technical plan for the grounding follow-up.
- Validation passed:
	- `bun run test src/scenes/environment/worldBuildingGrounding.test.ts src/scenes/environment/tenerifeRoadLayers.test.ts src/scenes/environment/puertoCityConfig.test.ts`
	- `bunx biome check src/scenes/environment/WorldBuildings.tsx src/scenes/environment/worldBuildingGrounding.ts src/scenes/environment/worldBuildingGrounding.test.ts src/scenes/environment/Environment.tsx`
	- `bun run check`
	- `bun run test`
	- `bun run build`
- Local HTTP smoke confirmed `http://127.0.0.1:5173/?tenerife=1&terrain=island-full` responds with a redirect to the configured base path.
- Browser screenshot QA could not run in this session because the in-app Browser `iab` backend is unavailable and Playwright is not installed in the workspace.

## Full-Island Roadside House Placement Follow-Up

- Responded to a second screenshot showing houses still visually suspended and some boxes placed over road ribbons.
- Reduced full-island generated placeholder scale from `1.55` to `1` so blockout houses no longer read as oversized slabs.
- Increased full-island generated house road setback from `0.85` to `2.4` and ground sink from `0.22` to `1.2`.
- Expanded footprint terrain sampling from `0.86` to `1.05` of collider footprint so lower outside edges on slopes are less likely to be missed.
- Added a road-surface rejection filter in `buildTenerifeRoadsideBuildings()` so candidates too close to another road segment are skipped instead of landing on intersecting or adjacent roads.
- Added focused clearance coverage proving transformed full-island placeholders keep more than `1.5` world units of clearance from widened main-road shoulders.
- Runtime-data smoke on `public/data/tenerife/roads-runtime.json` kept 84 transformed full-island houses with minimum calculated visual clearance of about `1.80` world units from widened road shoulders.
- Validation passed:
	- `bun run test src/scenes/environment/tenerifeRoadLayers.test.ts src/scenes/environment/worldBuildingGrounding.test.ts src/scenes/environment/puertoCityConfig.test.ts`
	- `bun run check`
	- `bun run test`
	- `bun run build`
- HTTP smoke confirmed `http://127.0.0.1:5173/?tenerife=1&terrain=island-full` still responds with a redirect to the configured base path.

## Full-Island Roadside House Vertical Retune

- Responded to a third screenshot showing the anti-floating fix overcorrected, with houses visibly buried into the terrain.
- Reduced full-island generated placeholder ground sink from `1.2` to `0.12`.
- Capped footprint support depth to `0.28` below the center terrain sample so a single low downhill sample does not pull the full box too far into the slope.
- Updated focused grounding coverage for the capped-support math.
- Runtime-data smoke on `public/data/tenerife/roads-runtime.json` still kept 84 transformed full-island houses with minimum calculated visual road clearance of about `1.80` world units; the worst transformed generated house now has `heightOffset` about `-0.093`.
- Validation passed:
	- `bun run test src/scenes/environment/worldBuildingGrounding.test.ts src/scenes/environment/tenerifeRoadLayers.test.ts src/scenes/environment/puertoCityConfig.test.ts`
	- `bunx biome check src/scenes/environment/Environment.tsx src/scenes/environment/tenerifeRoadLayers.ts src/scenes/environment/tenerifeRoadLayers.test.ts src/scenes/environment/worldBuildingGrounding.ts src/scenes/environment/worldBuildingGrounding.test.ts`
	- `bun run check`
	- `bun run test`
	- `bun run build`
- HTTP smoke confirmed `http://127.0.0.1:5173/?tenerife=1&terrain=island-full` still responds with a redirect to the configured base path.
- Browser screenshot QA could not run in this session because the in-app Browser `iab` backend returned an empty browser list.

## Full-Island Roadside House Scale And Slope-Gap Follow-Up

- Responded to a fourth report that the generated houses had become too small and still appeared to float above terrain.
- Raised full-island generated placeholder visual scale from `1` to `1.45`.
- Raised full-island generated placeholder ground sink from `0.12` to `0.35`.
- Raised the capped footprint support drop from `0.28` to `0.65`, keeping slope grounding stronger without returning to the earlier uncapped/deep embed.
- Updated `WorldBuildings` so terrain raycast grounding uses the same capped footprint support helper as heightfield grounding.
- Added a shallow visual-only foundation under `visualMode="boxes"` placeholder bodies to bridge small downhill gaps on sloped terrain while keeping player collision on the existing fallback/collider meshes.
- Runtime-data smoke on `public/data/tenerife/roads-runtime.json` kept 84 transformed full-island houses with minimum calculated visual road clearance of about `1.96` world units, minimum visible body height about `3.78`, average visible body height about `5.59`, and worst transformed `heightOffset` about `-0.311`.
- Validation passed:
	- `bun run test src/scenes/environment/worldBuildingGrounding.test.ts src/scenes/environment/tenerifeRoadLayers.test.ts src/scenes/environment/puertoCityConfig.test.ts`
	- `bunx biome check src/scenes/environment/Environment.tsx src/scenes/environment/WorldBuildings.tsx src/scenes/environment/tenerifeRoadLayers.test.ts src/scenes/environment/worldBuildingGrounding.ts src/scenes/environment/worldBuildingGrounding.test.ts`
	- `bun run check`
	- `bun run test`
	- `bun run build`
- HTTP smoke confirmed `http://127.0.0.1:5173/?tenerife=1&terrain=island-full` still responds with a redirect to the configured base path.
- Browser screenshot QA still could not run because the in-app Browser backend returned an empty browser list.
