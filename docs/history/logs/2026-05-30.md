# 2026-05-30

## Water Architecture Optimization Merge
- Merged `origin/water-architecture-optimization-epic-16335358440011621159` into `main`.
- Preserved the existing GitHub Pages deployment changes on `main`.
- Merge brought in water render-list optimization, shoreline path generation data/scripts, shoreline surf/sand updates, scene camera far-plane adjustment, and water entry effect cleanup.
- No merge conflicts were reported.

## GitHub Pages Deployment Follow-Up
- Confirmed the repository Pages source was switched to GitHub Actions after an initial deploy failure.
- Added this follow-up entry to trigger a fresh `main` push so `.github/workflows/deploy.yml` can deploy the built `dist` artifact through Actions.

## GitHub Pages Runtime Bundle Fix
- Investigated the production browser error `Class extends value undefined is not a constructor or null` from `thinEngine-DD9xwAxB.js`.
- Identified the generated Babylon `thinEngine` chunk extending a base class imported from the main app chunk, which can evaluate incorrectly under Vite/Rolldown chunk splitting.
- Updated `vite.config.ts` to set `build.rolldownOptions.output.codeSplitting` to `false`, keeping Babylon runtime modules in one production chunk.
- Verified `bun run build` now emits one app JS bundle instead of a separate `thinEngine` chunk.
- Ran local production preview and confirmed the startup inheritance error is gone. Existing absolute `/models/...` and `/textures/...` 404s remain as a separate asset base-path issue.

## GitHub Pages Public Asset Base Path Fix
- Investigated production 404s for `/models/...`, `/textures/...`, and runtime `/data/...` requests on GitHub Pages.
- Added `publicAssetUrl()` to resolve Vite `public/` assets under `import.meta.env.BASE_URL`.
- Updated Babylon model loaders, texture creation, and runtime JSON `fetch` calls to request assets below `/js-game-tf-lang-run/` in production.
- Added unit coverage for public asset URL normalization and updated Puerto city URL expectations.
- Verified local production preview requests the reported building OBJ files, Pumpkinboy GLB, and Ground068 textures from `/js-game-tf-lang-run/...` with 200/304 responses and no console errors.
- Follow-up published Pages smoke test showed building OBJ URLs were base-prefixed but missing from the deployed artifact because production pruning removed `.obj/.mtl` files.
- Updated `scripts/prune-public-assets.mjs` and tests to preserve the six runtime building OBJ files and their MTL materials in `dist`.

## References Used
- `docs/reference/tech-stack-validation.md`
- `docs/reference/runtime-architecture.md`
- `docs/reference/asset-pipeline.md`
- `docs/reference/scene-gameplay.md`
- `docs/reference/documentation-maintenance.md`

## Player Movement Physics Polish

Implemented 5 gameplay feel improvements based on physics/movement code review.

**New file: `src/scenes/player/playerMovementPhysics.ts`**
- `projectOntoSurface` — projects movement vector onto slope surface normal.
- `lerpVelocityXZ` — frame-rate-independent exponential lerp for XZ velocity.

**New file: `src/scenes/player/playerMovementPhysics.test.ts`** — 9 unit tests.

**Modified: `src/scenes/player/Player.tsx`**

| Phase | Change |
|---|---|
| 1 — Slope projection | `projectOntoSurface` applied when grounded; no-op on flat ground |
| 2 — Grounded check | Distance guard `PLAYER_GROUND_MAX_DISTANCE=1.35` replaces `Math.abs(vy)<=0.45` |
| 3 — Jump delay | 310 ms → 80 ms; queue expire 260 → 180 ms |
| 4 — Inertia | `lerpVelocityXZ` with `ACCEL=18`, `DECEL=24`; `smoothedHorizontalVelocityRef` |
| 5 — Air control | `PLAYER_AIR_CONTROL=0.35` scales direction when airborne |

Validation: `bun run test` 179/179, `bun run check` 0 errors, `bun run build` clean.

Epic: `TASKS/player-movement-physics-polish/`

### References Used
- `docs/reference/physics-collision.md`
- `docs/reference/scene-gameplay.md`
